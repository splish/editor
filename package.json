{
  "name": "@splish-me/editor-workspace",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "author": "Splish UG (haftungsbeschränkt)",
  "scripts": {
    "build": "lerna run --parallel build",
    "format": "prettier --write \"{packages/*/{src/**/*,*},*}.{js,jsx,ts,tsx,css,scss,json,md,markdown}\"",
    "start": "lerna run --parallel --scope @splish-me/editor-demo start",
    "predeploy": "yarn build",
    "deploy": "lerna publish"
  },
  "dependencies": {
    "material-ui": "0.19.4"
  },
  "devDependencies": {
    "lerna": "^3.2.1",
    "prettier": "^1.14.2"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ]
}
